import React from 'react';
import deviceGroup from './device_group.svg';

export default React.createClass({
  render() {
    return (
      <img src={deviceGroup} alt="Opsee devices" />
    );
  }
});